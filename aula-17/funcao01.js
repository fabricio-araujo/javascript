function parimpar() {
    if (n%2 == 0) {
        return 'Par!'
    } else {
        return 'Impar'
    }
}

let res = parimpar(4) //parimpar é a CHAMADA. 4 é o PARÂMETRO.
console.log(res)

// outra forma de escrever
console.log(parimpar(223)) //parimpar = chamada. 22 = parâmetro.